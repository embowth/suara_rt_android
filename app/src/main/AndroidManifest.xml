<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rw.rt.com.mykomplek">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MenuActivity">
        </activity>

        <activity android:name=".PemilihanActivity">
        </activity>

        <activity android:name=".LayananActivity">
        </activity>

        <activity android:name=".InfoActivity">
        </activity>

        <activity android:name=".DaruratActivity">
        </activity>

        <activity android:name=".ListInfoActivity">
        </activity>

        <activity android:name=".addInfoActivity">
        </activity>

        <activity android:name=".DetailInfoActivity">
        </activity>

        <activity android:name=".ReplyInfoActivity">
        </activity>

        <activity android:name=".LayananListActivity">
        </activity>

        <activity android:name=".LayananDetailActivity">
        </activity>

        <activity android:name=".ProfileActivity">
        </activity>

        <activity android:name=".ChangePasswordActivity">
        </activity>

        <activity android:name=".ChangeProfileActivity">
        </activity>

        <activity android:name=".PemilihanDetailActivity">
        </activity>
    </application>

</manifest>